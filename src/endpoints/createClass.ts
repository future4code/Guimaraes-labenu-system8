import {v4 as uuid} from "uuid"

class Class {
    id: string;
    name: string;
    module: string;

    constructor(id: string, name: string, module: string){
        this.id = id;
        this.name = name;
        this.module = module
    }



}